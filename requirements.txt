playwright>=1.49.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# CLI & dev
typer>=0.15.0
rich>=13.9.0
pytest>=8.3.0

# Email uses the Python stdlib (imaplib + smtplib) — no extra dependency.

# LLM (optional — parsing/reply fall back to regex+template when unset)
# openai>=1.60.0
# anthropic>=0.45.0
